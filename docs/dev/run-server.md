# Run from Sources

The BOSSS XDR Server can be run with three possible methods:
- **BOSSS XDR Server Binary** at `/usr/share/wazuh-server/bin/wazuh-server`.
- **Systemd** with the `systemctl start wazuh-server` command.
- **SysV init** with the `service wazuh-server start` command.